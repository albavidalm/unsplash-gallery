const Header = () => {
  return (
    <section className="header">
      <h1>Unsplash Gallery</h1>
      <button className="header__btn">Cargar nuevas fotos</button>
    </section>
  );
};

export default Header;
